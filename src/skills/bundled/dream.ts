/**
 * Register the /dream skill for memory consolidation.
 *
 * In production Anthropic Code, /dream distills daily append-only logs into
 * structured MEMORY.md files. For our initial KAIROS implementation, this
 * is a stub — the autoDream service handles background consolidation.
 */
export function registerDreamSkill() {
  // Stub: no-op. The autoDream service in src/services/autoDream/
  // handles background memory consolidation separately.
}
